name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

jobs:
  ci:
    uses: G3-Soluciones-IOT/shared-workflows/.github/workflows/java-ci.yml@main
    with:
      java-version: "25"
      working-directory: "."